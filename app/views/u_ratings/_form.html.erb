<%= form_for(@u_rating) do |f| %>
  <% if @u_rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@u_rating.errors.count, "error") %> prohibited this u_rating from being saved:</h2>

      <ul>
      <% @u_rating.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :uid %><br />
    <%= f.text_field :uid %>
  </div>
  <div class="field">
    <%= f.label :mid %><br />
    <%= f.text_field :mid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
