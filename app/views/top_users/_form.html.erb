<%= form_for(@top_user) do |f| %>
  <% if @top_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@top_user.errors.count, "error") %> prohibited this top_user from being saved:</h2>

      <ul>
      <% @top_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
