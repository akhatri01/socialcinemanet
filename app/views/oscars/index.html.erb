<div>
  <strong>Oscars</strong>
</div>
<table>
  <thead>
    <tr>
      <td>Oscar Category</td>
    </tr>
  </thead>

<% @oscars.each do |oscar| %>
  <tr>
    <td><%= link_to oscar.category, oscar_show_path(oscar.id) %></td>
  </tr>
<% end %>
</table>

<br />
